import {View, Text, StyleSheet} from 'react-native';
import React from 'react';

export default layoutEffect = StyleSheet.create({
  effectTitle: {
    fontSize: 25,
    fontFamily: 'times new roman',
    textAlign: 'center',
  },
  effectButton: {
    backgroundColor: '#57846E',
    padding: 10,
    marginTop: 10,
    borderRadius: 10,
  },
  effectButtonText: {
    fontSize: 18,
    color: '#fff',
    textAlign: 'center',
    fontFamily: 'times new roman',
  },
});
