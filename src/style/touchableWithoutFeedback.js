import {StyleSheet} from 'react-native';

export default btnWithoutFeedback = StyleSheet.create({
  button: {
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: 'green',
    padding: 10,
    margin: 10,
  },
});
